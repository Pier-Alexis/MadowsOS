
#include <stdio.h>
#include <stdlib.h>

void install_package(const char* package_name, const char* language) {
    printf("Installing %s for %s...\n", package_name, language);
    // Simulate downloading and installing
    printf("Package %s installed successfully!\n", package_name);
}
